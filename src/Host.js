import { View, Text} from "react-native";

const Host = ({ navigation }) => {
    return (
        <View>
            <Text>
                Host
            </Text>
        </View>
    );
};

export default Host;